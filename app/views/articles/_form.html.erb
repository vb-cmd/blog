<%= form_with(model: article) do |form| %>
  <% if article.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>
      <ul>
        <% article.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :title  %>
    <%= form.text_field :title , html: {required:true} %>
  </div>
  <div>
    <%= form.label :body  %>
    <%= form.textarea :body, html:{required:true}  %>
  </div>
  <div>
    <%= form.label :published  %>
    <%= form.checkbox :published , html: {required:true} %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
